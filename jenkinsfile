pipeline {
    agent any

    stages {
        stage('Checkout do código') {
            steps {
                git url:'https://github.com/Leonardo-009/conversao-temperatura.git', brach: 'main'
                sh 'ls'
            }
        }
    }
}
