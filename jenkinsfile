pipeline {
    agent any

    stages {
        stage('primeiro stage') {
            steps {
                echo 'primeira pipeline'
                // Adicione os comandos relacionados ao seu estágio aqui
            }
        }
    }
}
